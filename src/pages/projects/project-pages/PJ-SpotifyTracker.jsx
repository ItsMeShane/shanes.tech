import React from 'react';
import { ProjectPage } from './ProjectPage';

const images = [
   {
      src: '',
   },
];
const PJSpotifyTracker = () => {
   return <ProjectPage title='Spotify Tracker' images={images} />;
};

export default PJSpotifyTracker;
